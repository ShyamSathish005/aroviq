# Benchmarks

Aroviq is built for speed. We benchmark every release against standard datasets.

## v0.3.0 Benchmarks

<div className="mt-8 grid grid-cols-1 md:grid-cols-2 gap-8">

  <div className="p-6 border border-aroviq-cyan/30 bg-aroviq-cyan/5 rounded-xl">
    <h3 className="text-xl font-bold mb-4 text-aroviq-cyan">Tier 0 Latency</h3>
    <div className="text-4xl font-mono font-bold mb-2">0.42ms</div>
    <p className="text-sm text-gray-400">Mean processing time for Regex/Symbolic checks.</p>
  </div>

  <div className="p-6 border border-aroviq-purple/30 bg-aroviq-purple/5 rounded-xl">
    <h3 className="text-xl font-bold mb-4 text-aroviq-purple">False Positive Rate</h3>
    <div className="text-4xl font-mono font-bold mb-2">&lt; 1.2%</div>
    <p className="text-sm text-gray-400">On the standard "JailbreakBench" dataset.</p>
  </div>

</div>

## Comparison

| Guardrail | P99 Latency | Setup Time | Architecture |
| :--- | :--- | :--- | :--- |
| **Aroviq** | **180ms** (Hybrid) | 2 mins | Local + API |
| Guardrails AI | ~600ms | 15 mins | XML Config |
| NeMo Guardrails | ~800ms | 30 mins | Colang |

*Note: Latency includes Tier 1 LLM calls. Tier 0 only calls are sub-millisecond.*
